import { ComingSoonStep } from "@/components/onboarding/coming-soon-step";

export default function DashboardInitStepPage() {
  return (
    <ComingSoonStep
      step="dashboard"
      title="Dashboard Initialization"
      description="Your workspace is set up. Head into your dashboard — it'll keep filling in as sync finishes."
    />
  );
}
